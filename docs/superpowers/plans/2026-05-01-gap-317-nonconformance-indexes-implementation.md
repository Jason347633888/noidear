# GAP-317 NonConformance Indexes Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use `superpowers:executing-plans` to implement this plan task-by-task. Do not redesign nonconformance, CAPA, or query behavior. If current code disagrees with this plan, stop and report the mismatch to the main agent.

**Goal:** Add the missing Prisma indexes needed for high-volume `NonConformance` queries.

**Architecture:** This is a schema-only performance change. Add indexes matching current query and future polymorphic source lookup patterns without changing service behavior or DTO contracts.

**Tech Stack:** Prisma schema, Prisma generate/validate, optional migration generation.

---

## Superpower 与 grill-me 校准记录

- **Superpower 产出链路：** 主 agent 已按项目文档和 `writing-plans` 要求完成上下文核对、范围收敛和 implementation plan 编写。
- **grill-me 校准结论：** 已质询是否需要改查询、是否需要新增字段、是否影响历史数据。结论是只加索引，不改数据语义，不迁移业务数据。
- **执行限制：** Multica 执行 agent 只能使用 `superpowers:executing-plans` 执行本计划；不得自行扩展范围、补写新 spec、重排 GAP 或改动未列入文件。
- **执行隔离：** 执行本计划前必须从最新 `origin/master` 创建独立 worktree，或使用 Multica 隔离工作目录；不得在主 checkout `/Users/jiashenglin/Desktop/好玩的项目/noidear` 直接修改、提交或 push。如发现当前目录是主 checkout，必须停止并回报主 agent。
- **停止条件：** 如果执行 agent 发现本计划与当前代码、AGENTS.md、docs/AGENT_GUIDE.md 或 docs/MASTER_DATA_AND_TRACEABILITY_MODEL.md 冲突，必须停止并回报主 agent，不得猜测实现。

## File Map

- Modify: `server/src/prisma/schema.prisma`
- Add migration only if the repo's existing Prisma workflow requires a checked-in migration.
- Do not modify: `server/src/modules/non-conformance/non-conformance.service.ts`

## Task 1: Add NonConformance indexes

**Files:**
- Modify: `server/src/prisma/schema.prisma`

- [ ] **Step 1: Locate `model NonConformance`**

The model currently ends with:

```prisma
  @@unique([company_id, nc_no])
}
```

- [ ] **Step 2: Add the two indexes below the unique constraint**

```prisma
  @@unique([company_id, nc_no])
  @@index([company_id, status])
  @@index([company_id, source_type, source_id])
}
```

- [ ] **Step 3: Confirm no service behavior changed**

```bash
git diff -- server/src/modules/non-conformance
```

Expected: no diff.

## Task 2: Validate Prisma schema

**Files:**
- Read/check only unless migration files are generated by command.

- [ ] **Step 1: Validate the schema**

```bash
cd /Users/jiashenglin/Desktop/好玩的项目/noidear/server
npx prisma validate --schema src/prisma/schema.prisma
```

Expected: schema is valid.

- [ ] **Step 2: Generate Prisma client**

```bash
cd /Users/jiashenglin/Desktop/好玩的项目/noidear/server
npx prisma generate --schema src/prisma/schema.prisma
```

Expected: Prisma client generation succeeds.

- [ ] **Step 3: If the project requires migrations, create a migration**

Only do this if the repository has existing committed Prisma migration directories and local database configuration is available:

```bash
cd /Users/jiashenglin/Desktop/好玩的项目/noidear/server
npx prisma migrate dev --schema src/prisma/schema.prisma --name add_nonconformance_indexes --create-only
```

Expected: a migration is generated with `CREATE INDEX` statements for the two new indexes. If local DB env is unavailable, do not fake a migration; report that migration generation needs DB access.

## Task 3: Focused tests

- [ ] **Step 1: Run existing nonconformance unit tests**

```bash
cd /Users/jiashenglin/Desktop/好玩的项目/noidear/server
npm test -- non-conformance.service.spec.ts
```

Expected: PASS.

- [ ] **Step 2: Commit**

```bash
git add server/src/prisma/schema.prisma
git add server/src/prisma/migrations 2>/dev/null || true
git commit -m "fix: add nonconformance query indexes"
```
