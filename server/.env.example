# 数据库配置
# 格式: postgresql://用户名:密码@主机:端口/数据库名
# 生产环境必须使用强密码（至少 16 位，包含大小写字母+数字+特殊字符）
DATABASE_URL="postgresql://noidear:your_strong_password_here@localhost:5432/document_system"

# JWT 配置
# JWT_SECRET: 用于签名 JWT token 的密钥
# 生产环境必须使用至少 32 位的随机字符串
# 生成方法: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET="your_jwt_secret_at_least_32_characters_long"
JWT_EXPIRES_IN="7d"

# Redis 配置
REDIS_URL="redis://localhost:6380"

# MinIO 对象存储配置
MINIO_ENDPOINT="localhost"
MINIO_PORT=9000
MINIO_USE_SSL=false
MINIO_ACCESS_KEY="your_minio_access_key"
MINIO_SECRET_KEY="your_minio_secret_key_at_least_16_chars"

# CORS 配置（可选）
# 生产环境设置为前端域名，如: https://app.example.com
# CORS_ORIGIN="http://localhost:5173"

# ElasticSearch 配置（可选，未配置时降级为 PostgreSQL 全文搜索）
# Docker Compose 启动命令: docker compose --profile elasticsearch up -d
# ELASTICSEARCH_URL=http://elasticsearch:9200

# LDAP 配置（可选，未配置时 LDAP 登录不可用）
# LDAP_URL=ldap://your-ldap-server:389
# LDAP_BIND_DN=CN=service-account,DC=company,DC=com
# LDAP_BIND_PASSWORD=your-service-account-password
# LDAP_BASE_DN=DC=company,DC=com
